.mainBox
  .sideBar
    .sideBarTop
      .sideBarTop__userName
        = current_user.name
      = link_to edit_user_path(current_user) do
        %i.fa.fa-cog{"aria-hidden" => "true"}
      = link_to new_group_path do
        %i.fa.fa-pencil-square-o{"aria-hidden" => "true"}
    .sideBarSpaceArea
    .chatHistory
      = render "shared/group-each"
